import { Container } from './styles';

export default function MoreInformations({ movie }) {
  return (
    <Container>
      <p>{movie.title}</p>
      <p>{movie.title}</p>
      <p>{movie.title}</p>
      <p>{movie.title}</p>
      <p>{movie.title}</p>
    </Container>
  );
}
